#!/usr/bin/env python

from wander import *

if __name__ == "__main__":
    zimbra_settings = dict(read_config('zimbra'))
    fabric_settings = dict(read_config('fabric'))
    imap_settings = dict(read_config('imap'))

    imap = imap_connect(imap_settings, 'kharriss')
    print imap.list()
